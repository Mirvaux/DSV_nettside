export default function PostTitle({ children }) {
  return (
    <h1 className="font-cormorant mb-12 text-center text-6xl leading-tight tracking-wide md:text-left md:text-6xl md:leading-none lg:text-8xl">
      {children}
    </h1>
  )
}
